<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Idle Timeout (นาที)
    |--------------------------------------------------------------------------
    |
    | ระยะเวลาที่ยอมให้ผู้ใช้ที่ login แล้วไม่มีกิจกรรม (idle) ก่อนถูก
    | auto logout — แยกค่าระหว่างฝั่ง User (หน้าบ้าน) และ Admin (หลังบ้าน)
    | ตาม design.md หัวข้อ 2.1 (Admin เข้มกว่าเพราะจัดการข้อมูลสมาชิก/สิทธิ์)
    |
    */

    'user_minutes' => (int) env('IDLE_TIMEOUT_USER', 60),

    'admin_minutes' => (int) env('IDLE_TIMEOUT_ADMIN', 30),

];
